This directory stores historical implementation snapshots that are not part of the active Next.js app.

- `legacy-cra-home/`: pre-Next CRA-based home/app source kept for migration reference only

Do not edit archived files for product changes. Update the live app under `app/`, `components/`, `content/`, and `lib/`.
